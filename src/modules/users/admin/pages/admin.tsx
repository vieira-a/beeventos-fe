import { AdminEventsData } from '../../components/admin-events-data';

export const Admin = () => {
  
  return (
    <section className='flex flex-col gap-4'>
      <AdminEventsData />
  </section>
  )
}